
<%= simple_form_for [@batiment,@piece] do |f|  %>
    <%= f.input :nom, collection: f.object.assignable_noms, :label => 'Type de Pièce' %>
    <%= f.input :numero, label: 'Numéro (si plusieurs)' %>
    <%= f.button :submit, :class => 'btn btn-info', label: 'Créer la pièce' %>
<% end %>